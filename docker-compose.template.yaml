version: "3"

services:
  browser1:
    image: lscr.io/linuxserver/chromium:latest
    container_name: browser1
    shm_size: "2gb"
    ports:
      - "3001:3000"
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ./data/browser1:/config
